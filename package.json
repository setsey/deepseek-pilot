{
  "name": "deepseek-v4-qa",
  "displayName": "DeepSeek V4 QA — Vision + Token Tracking",
  "description": "DeepSeek V4 Pro & Flash in GitHub Copilot Chat. Vision proxy for image attachments, KV-cache-aware context-window indicator, persistent reasoning cache, live session cost and platform balance in the status bar, a thinking-effort selector per model variant, and one-click wiring as Copilot's utility/utility-small model.",
  "icon": "resources/icon.png",
  "version": "0.2.0",
  "publisher": "konstantyn-ganenkov",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "local"
  },
  "engines": {
    "vscode": "^1.120.0",
    "node": ">=24"
  },
  "extensionDependencies": [
    "github.copilot-chat"
  ],
  "extensionKind": [
    "workspace"
  ],
  "categories": [
    "AI",
    "Chat",
    "Programming Languages"
  ],
  "keywords": [
    "deepseek",
    "deepseek v4",
    "deepseek-v4",
    "copilot",
    "github copilot",
    "copilot chat",
    "vision",
    "token tracking",
    "balance",
    "byok",
    "language model",
    "thinking",
    "reasoning",
    "ai",
    "chat"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "enabledApiProposals": [],
  "main": "./out/extension.js",
  "contributes": {
    "languageModelChatProviders": [
      {
        "vendor": "deepseek-qa",
        "displayName": "DeepSeek V4 QA",
        "managementCommand": "deepseek-qa.manage"
      }
    ],
    "commands": [
      {
        "command": "deepseek-qa.manage",
        "title": "DeepSeek QA: Manage Provider"
      },
      {
        "command": "deepseek-qa.setApiKey",
        "title": "DeepSeek QA: Set API Key"
      },
      {
        "command": "deepseek-qa.getApiKey",
        "title": "DeepSeek QA: Get API Key"
      },
      {
        "command": "deepseek-qa.clearApiKey",
        "title": "DeepSeek QA: Clear API Key"
      },
      {
        "command": "deepseek-qa.setVisionModel",
        "title": "DeepSeek QA: Set Vision Proxy Model"
      },
      {
        "command": "deepseek-qa.showLogs",
        "title": "DeepSeek QA: Show Logs"
      },
      {
        "command": "deepseek-qa.refreshBalance",
        "title": "DeepSeek QA: Refresh Balance"
      },
      {
        "command": "deepseek-qa.clearSession",
        "title": "DeepSeek QA: Clear Session Counter"
      },
      {
        "command": "deepseek-qa.showCacheStats",
        "title": "DeepSeek QA: Show Reasoning Cache Stats"
      },
      {
        "command": "deepseek-qa.showContextWindow",
        "title": "DeepSeek QA: Show Context Window Details"
      },
      {
        "command": "deepseek-qa.setUtilityModel",
        "title": "DeepSeek QA: Use as Copilot Utility Model"
      },
      {
        "command": "deepseek-qa.setUtilitySmallModel",
        "title": "DeepSeek QA: Use as Copilot Utility Small Model"
      },
      {
        "command": "deepseek-qa.clearReasoningCache",
        "title": "DeepSeek QA: Clear Reasoning Cache"
      }
    ],
    "configuration": {
      "title": "DeepSeek V4 QA",
      "properties": {
        "deepseek-qa.baseUrl": {
          "type": "string",
          "default": "https://api.deepseek.com",
          "description": "DeepSeek API base URL."
        },
        "deepseek-qa.maxTokens": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "description": "Maximum output tokens per request. 0 = API default (no limit)."
        },
        "deepseek-qa.reasoningEffort": {
          "type": "string",
          "enum": [
            "high",
            "max"
          ],
          "enumDescriptions": [
            "Faster responses with shorter reasoning chains.",
            "Maximum reasoning depth; slower and uses more tokens."
          ],
          "default": "max",
          "markdownDescription": "Reasoning effort for `(thinking)` model variants. Has no effect on non-thinking variants."
        },
        "deepseek-qa.debug": {
          "type": "boolean",
          "default": false,
          "description": "Enable diagnostic debug logging."
        },
        "deepseek-qa.modelIdOverrides": {
          "type": "object",
          "default": {
            "deepseek-v4-flash": "deepseek-v4-flash",
            "deepseek-v4-pro": "deepseek-v4-pro"
          },
          "description": "Optional API model ID overrides for DeepSeek-compatible proxy endpoints.",
          "additionalProperties": false,
          "properties": {
            "deepseek-v4-flash": {
              "type": "string",
              "description": "API model ID to use for DeepSeek V4 Flash variants."
            },
            "deepseek-v4-pro": {
              "type": "string",
              "description": "API model ID to use for DeepSeek V4 Pro variants."
            }
          }
        },
        "deepseek-qa.visionModel": {
          "type": "string",
          "default": "",
          "description": "Model ID for vision proxy (describes images for text-only DeepSeek). Leave empty for auto-detection. Use 'DeepSeek QA: Set Vision Proxy Model' to pick from a list."
        },
        "deepseek-qa.visionPrompt": {
          "type": "string",
          "editPresentation": "multilineText",
          "default": "Describe the visual contents of this image in detail, including any text, objects, people, or context that would be relevant for understanding it. Focus on factual visual elements.",
          "description": "Prompt sent to the vision proxy model when describing image attachments."
        },
        "deepseek-qa.applyProDiscount": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Apply DeepSeek-V4-Pro's 75% promotional discount when estimating session cost. Auto-expires after the discount window ends (2026-05-31). Off by default so the displayed cost is an upper bound matching post-promo billing."
        },
        "deepseek-qa.contextWarnThreshold": {
          "type": "number",
          "default": 60,
          "minimum": 10,
          "maximum": 95,
          "markdownDescription": "Percent of the model's context window at which the context-window status bar item turns yellow (warning). Below this, the indicator stays neutral and advises against compacting — DeepSeek's KV cache makes compaction expensive when there's still room."
        },
        "deepseek-qa.contextCriticalThreshold": {
          "type": "number",
          "default": 80,
          "minimum": 20,
          "maximum": 99,
          "markdownDescription": "Percent of the model's context window at which the context-window status bar item turns red (critical) and recommends compacting immediately to avoid truncation. Must be greater than `deepseek-qa.contextWarnThreshold`."
        }
      }
    },
    "walkthroughs": [
      {
        "id": "deepseekQaGettingStarted",
        "title": "DeepSeek V4 QA — Setup",
        "description": "Set up your DeepSeek API key and start using V4 models in Copilot Chat with vision + token tracking.",
        "steps": [
          {
            "id": "setApiKey",
            "title": "Set your DeepSeek API key",
            "description": "[Get an API key](https://platform.deepseek.com/api_keys) from platform.deepseek.com.\n[Set API Key](command:deepseek-qa.setApiKey)",
            "media": {
              "markdown": "resources/walkthrough/set-api-key.md"
            },
            "completionEvents": [
              "onCommand:deepseek-qa.setApiKey"
            ]
          },
          {
            "id": "pickModel",
            "title": "Pick a DeepSeek V4 model",
            "description": "Open Copilot Chat and select a DeepSeek V4 QA variant from the model picker. Four variants available: Pro/Flash × thinking/non-thinking. If they are hidden, use Manage Provider from the picker menu.",
            "media": {
              "markdown": "resources/walkthrough/pick-model.md"
            }
          },
          {
            "id": "tuneReasoningEffort",
            "title": "Tune reasoning effort (optional)",
            "description": "Switch between `high` and `max` for `(thinking)` variants. [Open Reasoning Effort Setting](command:workbench.action.openSettings?%22deepseek-qa.reasoningEffort%22)",
            "media": {
              "markdown": "resources/walkthrough/tune-effort.md"
            }
          }
        ]
      }
    ]
  },
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('out', { recursive: true, force: true, maxRetries: process.platform === 'win32' ? 10 : 0 })\"",
    "vscode:prepublish": "npm run compile",
    "compile": "npm run clean && tsc -p ./",
    "watch": "npm run clean && tsc -watch -p ./",
    "lint": "npx oxlint",
    "format": "npx oxfmt --write src/",
    "package": "node scripts/package.mjs",
    "publish": "npx @vscode/vsce publish"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/vscode": "^1.120.0",
    "@vscode/vsce": "^3.9.1",
    "oxfmt": "^0.47.0",
    "oxlint": "^1.62.0",
    "typescript": "^6.0.3"
  }
}
